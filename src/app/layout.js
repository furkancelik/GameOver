export const metadata = {
  title: "Gmover Farming",
  description: "Gmover Farming & Stake Game",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
